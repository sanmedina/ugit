[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "ugit"
version = "1.0"
description = "DIY git system"
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}

[project.scripts]
ugit = "ugit.cli:main"